# Triton

对应 `W09-W16`。

这里建议拆成两个阶段：

- 入门：`vector add`、`fused softmax`、`matmul`、`persistent matmul`
- 进阶：`RMSNorm`、`online softmax`、`attention forward`

推荐目录结构：

- `python/`
- `benchmarks/`
- `notes/`
