# CUTLASS Examples

Current examples:

- `cutlass_header_probe.cu`: verifies CUTLASS headers, CUDA runtime, and visible GPU compute capabilities.

Planned examples:

- official quickstart GEMM wrapper
- `GemmUniversal` configuration probe
- fused epilogue `bias+relu`
- fused epilogue `bias+silu`
