# TVM

v3 计划调整为 **P2 延后学习**：W52 后按岗位需要启动，不作为投递前置。详见 [更新后的学习计划](../../docs/learning-plan-w29-w52.md)。

建议在这里把 TVM 路线分成两层：

- `intro/`：Quick Start、TensorIR、MetaSchedule
- `advanced/`：schedule 进阶、端到端 pipeline

与 RK3588 联动的部署部分可放到 `edge/rk3588`。
